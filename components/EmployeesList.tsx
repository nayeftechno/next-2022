import {useEffect,useState} from 'react';
import axios from 'axios';

function EmployeesList():JSX.Element{
    const [state,setState] = useState({loading : false,employees : []});
    return(<h1></h1>);
};

export default EmployeesList